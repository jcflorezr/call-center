package net.learningpath.callcenter;

import org.junit.Test;

import static org.junit.Assert.*;

public class ControllerTest {

    @Test
    public void getRequest() {

        Controller controller = new Controller();

        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);
        controller.getRequest(null);




    }
}